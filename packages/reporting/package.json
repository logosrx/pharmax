{
  "name": "@pharmax/reporting",
  "_comment_dependencies": "cron-parser is a tiny zero-deps cron tool used by CreateReportSchedule (validate expression + compute initial nextRunAt) and by the worker tick (compute next nextRunAt after each run).",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pharmax/command-bus": "workspace:*",
    "@pharmax/database": "workspace:*",
    "@pharmax/platform-core": "workspace:*",
    "@pharmax/rbac": "workspace:*",
    "cron-parser": "^4.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.19.20",
    "typescript": "^5.6.3"
  }
}
